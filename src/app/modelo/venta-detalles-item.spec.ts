import { VentaDetallesItem } from './venta-detalles-item';

describe('VentaDetallesItem', () => {
  it('should create an instance', () => {
    expect(new VentaDetallesItem()).toBeTruthy();
  });
});
